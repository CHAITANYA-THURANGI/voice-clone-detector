<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.voiceshield">

    <!-- Telephony & Call Screening Permissions (Truecaller-Style) -->
    <uses-permission android:name="android.permission.ANSWER_PHONE_CALLS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <!-- Audio & Network for Real-Time AI Forensics -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="VoiceShield AI"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VoiceShield"
        android:usesCleartextTraffic="true">

        <!-- Main Settings & Permission Setup Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 1. TRUECALLER-STYLE CALL SCREENING SERVICE (Pre-Call Ring Blocker) -->
        <!-- Intercepts incoming calls BEFORE the phone rings to reject known scammers -->
        <service
            android:name=".VoiceShieldCallScreeningService"
            android:permission="android.permission.BIND_SCREEN_CALL_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.telecom.CallScreeningService" />
            </intent-filter>
        </service>

        <!-- 2. IN-CALL REAL-TIME DEEPFAKE & SCAM DEFENSE SERVICE (Mid-Call AI Interceptor) -->
        <!-- Intercepts audio chunks during active call and triggers call.disconnect() on attack -->
        <service
            android:name=".VoiceShieldInCallService"
            android:permission="android.permission.BIND_INCALL_SERVICE"
            android:exported="true">
            <intent-filter>
                <action android:name="android.telecom.InCallService" />
            </intent-filter>
        </service>

    </application>
</manifest>
